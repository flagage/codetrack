@model MvcCodetrace.Models.Cave

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Cave</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Proprietaire)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Proprietaire)
            @Html.ValidationMessageFor(model => model.Proprietaire)
        </div>

        @Html.HiddenFor(model => model.Id)

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
